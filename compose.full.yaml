# Explicit full-stack variant overlay.
#
# The current canonical compose.yaml already contains the local Postgres,
# local MinIO and init-SQL topology. This file exists so operators can use the
# same variant command shape as external variants while retaining backward
# compatibility with `docker compose -f compose.yaml`.
services: {}
