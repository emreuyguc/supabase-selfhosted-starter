# Supabase AI Guide

This is the Supabase-specific AI and maintainer guide for this repository.

Use it together with `docs/STARTER_KIT_AI_RULES.md`. The generic rules define how starter-kit repositories should behave; this file defines the concrete source-of-truth and Supabase-specific constraints for this repo.

## Repository identity

- This is a Supabase-only self-hosted starter.
- Do not convert it into a multi-app monorepo.
- Prefer upstream Supabase-compatible images and configs.
- Keep local Compose, Dokploy and generated docs synchronized.

## Current source of truth

| Area | Source |
|---|---|
| Canonical stack | `compose.yaml` |
| Full-stack variant overlay | `compose.full.yaml` |
| External DB variant overlay | `compose.external-db.yaml` |
| External preconfigured variant overlay | `compose.external-prebuilt.yaml` |
| External S3 feature overlay | `compose.features.external-s3.yaml` |
| Production override | `compose.prod.yaml` |
| Env inventory | `.env.example` |
| Variant env examples | `.env.full.example`, `.env.external-db.example`, `.env.external-prebuilt.example`, `.env.external-s3.example` |
| Runtime bind files | `files/` |
| Service/image inventory | `manifest.yaml` |
| Dokploy source config | `dokploy/config.toml` |
| Dokploy render commands | `dokploy/Makefile` |
| Dokploy generated artifacts | `dokploy/templates/*.json` |
| Dokploy variant artifacts | `dokploy/templates/*.json` |
| Runtime tests | `tests/runtime/` |
| Render/validation scripts | `scripts/` |
| README generated summary | generated from `manifest.yaml` by `scripts/render-readme.sh` |
| Documentation policy | `docs/DOCUMENTATION_STRUCTURE.md` |

Generated artifacts:

- `dokploy/templates/*.json` are generated by `scripts/render-dokploy.sh`.
- Do not edit generated artifacts directly unless the renderer changes in the same commit.

## Required workflow for AI agents

1. Run or inspect `git status --short`.
2. Determine whether the request affects Compose, env, files, platform artifacts, tests, scripts or docs.
3. Update all synchronized artifacts, not just the file that looks directly related.
4. If `compose.yaml` or `files/` changed, run:

   ```sh
   make -C dokploy render-all
   ```

5. If `manifest.yaml` or README summary data changed, run:

   ```sh
   ./scripts/render-readme.sh
   ```

6. Always run:

   ```sh
   ./scripts/validate.sh
   ```

7. If a live deployment is available and the user authorizes runtime testing, run:

   ```sh
   npm --prefix tests/runtime test
   ```

8. Report what changed, what was validated and what still needs fresh deploy/runtime testing.

## Supabase-specific constraints

- Kong is the only public ingress by default.
- Postgres, MinIO, Logflare, pg-meta and internal service ports must not be publicly exposed by default.
- Studio is exposed through Kong and protected by basic auth.
- Modern opaque keys are supported at Kong and mapped internally to legacy Supabase JWT keys.
- Do not add gateway `key-auth` to Storage routes if it breaks S3/SigV4 compatibility.
- GraphQL is enabled through `pg_graphql` and `graphql_public.graphql`.
- `pg_cron` is enabled with admin-focused permissions.
- External DB bootstrap requires the external provider to expose `pg_net`, `pg_graphql` and `pg_cron`. Do not present generic vanilla Postgres as supported unless those extensions are available and `pg_cron` provider configuration is handled.
- SMTP and phone signup are disabled by default.
- Public signup defaults must remain safe until real SMTP/SMS configuration exists.
- Admin and migration routes must require service/admin credentials.
- `supabase-vector` uses a read-only Docker socket mount for Docker log collection. Keep it documented as an intentional operational/security exception.

## Env source-of-truth

`.env.example` is the full non-secret inventory.

Env is value inventory, not topology control. It may configure URLs, credentials, feature flags and runtime settings inside the selected variant, but it must not decide whether local Postgres, init SQL, MinIO, bootstrap jobs or migrations exist.

Variant env examples document the values required by each topology:

| File | Purpose |
|---|---|
| `.env.full.example` | Local full-stack Supabase with local Postgres, init SQL and MinIO |
| `.env.external-db.example` | Runtime services connected to an external database, with bootstrap SQL handled explicitly |
| `.env.external-prebuilt.example` | Runtime services connected to a preconfigured/restored external Supabase database; no bootstrap or init SQL |
| `.env.external-s3.example` | External S3 object storage values for the external S3 feature overlay |

Real env generation:

```sh
SUPABASE_PUBLIC_URL=http://example.com ./scripts/generate-env.sh --variant full
```

The generator:

- refuses to overwrite `.env` unless `FORCE=1`;
- accepts an explicit variant and writes one concrete `.env` for that selected variant;
- creates strong secret values;
- creates anon and service-role JWTs signed by the generated JWT secret;
- keeps required alias values identical.

Important aliases:

| Canonical value | Required aliases |
|---|---|
| Postgres password | `POSTGRES_PASSWORD`, `SERVICE_PASSWORD_POSTGRES` |
| JWT secret | `JWT_SECRET`, `SERVICE_PASSWORD_JWT` |
| Anon key | `ANON_KEY`, `SERVICE_SUPABASEANON_KEY` |
| Service role key | `SERVICE_ROLE_KEY`, `SERVICE_SUPABASESERVICE_KEY` |
| Dashboard user | `DASHBOARD_USERNAME`, `SERVICE_USER_ADMIN` |
| Dashboard password | `DASHBOARD_PASSWORD`, `SERVICE_PASSWORD_ADMIN` |

Storage uses one canonical env namespace: `STORAGE_S3_*` for backend settings and
`MINIO_ROOT_*` for the local MinIO credentials. Do not add `GLOBAL_S3_*` or
service-prefixed MinIO aliases.

## Deployment variant contract

Use Compose overlays for mutually exclusive Supabase topologies:

| Variant | Compose files | Rule |
|---|---|---|
| `full` | `compose.yaml` + `compose.full.yaml` | Runs local Postgres, local MinIO and required init SQL/bootstrap behavior |
| `external-db` | `compose.yaml` + `compose.external-db.yaml` | Connects to external Postgres, runs `supabase-db-bootstrap`, then starts runtime services and service migrations |
| `external-prebuilt` | `compose.yaml` + `compose.external-prebuilt.yaml` | Connects to an already configured/restored Supabase database; local Postgres, init SQL and Realtime/Supavisor migration commands are absent, but Supavisor/pooler is still managed by this stack |

Use profiles only for additive optional services. Do not use profiles for mutually exclusive database/storage topology choices.

External S3 is an additive feature overlay, not a main deployment variant. It disables local MinIO and `minio-createbucket`, then points Storage at an existing S3-compatible backend.

Supavisor is managed by this stack in all supported DB variants. Do not assume an external pooler for `external-db` or `external-prebuilt`. If external pooler support is ever needed, it must be a separate explicit feature overlay.

`external-db` uses `supabase-db-bootstrap` as a one-shot Compose job. If the bootstrap SQL changes, the job fails halfway, or the external database target changes, operators must explicitly recreate/rerun the bootstrap job before relying on runtime services.

Examples:

```sh
docker compose --env-file .env -f compose.yaml -f compose.full.yaml up -d
docker compose --env-file .env -f compose.yaml -f compose.external-db.yaml up -d
docker compose --env-file .env -f compose.yaml -f compose.external-prebuilt.yaml up -d
docker compose --env-file .env -f compose.yaml -f compose.external-db.yaml -f compose.features.external-s3.yaml up -d
```

Production policy remains an additional overlay:

```sh
docker compose --env-file .env -f compose.yaml -f compose.full.yaml -f compose.prod.yaml up -d
```

Dokploy support should render one concrete import artifact per supported variant. Users should import the matching generated artifact instead of editing Compose or JSON by hand.

## Static validation contract

`scripts/validate.sh` must validate:

- current root/full Compose syntax;
- supported variant overlay syntax;
- production override syntax for supported variants;
- Dokploy import JSON;
- embedded Dokploy Compose syntax for generated artifacts;
- required bind-mounted files.

CI must fail if generated Dokploy or README artifacts drift from their renderers.

## Runtime test contract

Runtime tests live in `tests/runtime/` and use Vitest with TypeScript.

Runtime test usage is documented in `tests/runtime/README.md`.

The suite should cover:

- Studio basic auth;
- Auth health;
- signup disabled defaults;
- admin user create/delete;
- REST OpenAPI and CRUD;
- GraphQL introspection;
- Storage bucket/object CRUD;
- signed URL path;
- image transformation path;
- Edge Function hello route;
- Realtime WebSocket/channel path;
- pg-meta admin ACL;
- `pg_cron` schedule/list/unschedule.

Runtime tests are allowed to create temporary resources and must clean them up.

Static test-package validation:

```sh
npm --prefix tests/runtime run typecheck
```

## Platform notes

### Dokploy

Dokploy has generated import artifacts:

```text
dokploy/templates/full-local.json
dokploy/templates/full-external-s3.json
dokploy/templates/external-db-local.json
dokploy/templates/external-db-external-s3.json
dokploy/templates/external-prebuilt-local.json
dokploy/templates/external-prebuilt-external-s3.json
```

Render it with:

```sh
make -C dokploy render-all
make -C dokploy render-template VARIANT=external-db STORAGE=external-s3
```

The renderer:

- reads `compose.yaml`;
- applies supported variant topology transforms;
- reads `dokploy/config.toml`;
- rewrites `./files/...` to `../files/...`;
- embeds `files/` content into `[[config.mounts]]`;
- runs static validation.

Operational Dokploy usage is documented in `dokploy/README.md`.

### Coolify

This repository does not keep a separate Coolify artifact.

Use root `compose.yaml` as the manual/user-defined Compose base. This is not a Coolify one-click catalog template. Operators must ensure `files/` exists relative to the deployment bundle or adapt bind paths on the target platform.

## Branch and versioning policy

This repository uses simple project-specific versioning.

Branches:

- `main`: canonical stable development branch.
- `feature/<short-scope>`: scoped feature work.
- `fix/<short-scope>`: scoped bug/security/config fixes.
- `docs/<short-scope>`: documentation-only changes.
- `experiment/<short-scope>`: disposable trials that must not be treated as release candidates.

Tags:

- Use immutable tags for known-good states.
- Recommended tag shape: `supabase-starter-vX.Y.Z`.
- If a tag is tied to a major upstream stack refresh, use: `supabase-starter-vX.Y.Z-upstream-YYYY.MM.DD`.

Version source:

- `manifest.yaml` owns `starter_version` and `last_updated`.
- `manifest.yaml` owns service image tags and public access metadata.
- `README.md` displays generated version/service summary from `manifest.yaml`.

Version bump rules:

- Patch bump: docs, tests, script fixes, non-breaking config cleanup.
- Minor bump: new supported feature, new platform integration, materially improved production behavior.
- Major bump: breaking env/volume/network/template behavior or major Supabase stack migration.

Release checklist:

1. Update image tags and metadata in `manifest.yaml`.
2. Run `make render-readme`.
3. Run `make -C dokploy render-all` if Compose or files changed.
4. Run `make validate`.
5. Run runtime smoke tests when a deployment is available.
6. Create an immutable git tag only after static validation and, for production-ready states, runtime/fresh deploy testing.

## Backup/restore contract

Stateful services currently requiring backup coverage:

- Postgres named volumes;
- MinIO named volume.

Scripts:

- `scripts/backup-postgres.sh`
- `scripts/restore-postgres.sh`
- `scripts/backup-minio.sh`
- `scripts/restore-minio.sh`

Restore scripts must remain guarded by `CONFIRM_RESTORE=1`.

Script usage and safety behavior are documented in `scripts/README.md`.

## Change risk map

| Change | Must also check |
|---|---|
| `compose.yaml` | Dokploy render, manifest, README/user guide, validation |
| `.env.example` | env generator, Dokploy config, tests env, user guide |
| `files/volumes/api/kong.yml` | route ACLs, Storage S3/SigV4 behavior, security tests |
| DB init SQL | fresh deploy test, GraphQL/cron/auth/storage tests |
| Storage config | bucket/object tests, signed URL, image transform, MinIO backup docs |
| Auth config | signup defaults, SMTP notes, admin user tests |
| Image tag | manifest, README service table, runtime smoke test |
| Documentation structure | `docs/DOCUMENTATION_STRUCTURE.md` |
| Script behavior | `scripts/README.md`, Makefile, CI |
| Dokploy behavior | `dokploy/README.md`, render script, generated template |
| Runtime test behavior | `tests/runtime/README.md`, package scripts, CI typecheck |

## Do not

- Do not add unused folders.
- Do not commit real secrets.
- Do not add Dockerfiles unless official images are insufficient.
- Do not rely on hidden Compose fallbacks for production-critical env.
- Do not use env variables to select whether local DB, bootstrap/migration or storage services exist.
- Do not expose internal services publicly.
- Do not weaken admin ACLs for convenience.
- Do not mark the stack production-ready after static validation only; fresh deploy and runtime tests are separate.
